---
"ui": patch
---

Differentiate the "Pending Review" dashboard status from the approved state by switching its accent from `brand-green` (which resolves to the same `#00d9a3` mint as `--brand-cyan`) to `brand-cobalt`, so builders no longer misread a pending submission as approved (#141).