---
"ui": minor
---

Edit social links on the builder profile.

- Add website, GitHub, X, Telegram, and LinkedIn fields to the builder profile editor, with URL validation and error handling.
- Prefill the link fields from the builder's NEAR Social profile (handles are normalized to full URLs) so existing links can be kept, edited, or cleared.
- On the profile page, hide social link badges whose value is empty.
