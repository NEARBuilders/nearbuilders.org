---
"ui": patch
---

Replace the unreplaced build-template `| app` suffix on the `/projects` and `/about` page titles with `| NEAR Builders` to match the convention used by `/events`, `/activity`, and `/builders` (#121).