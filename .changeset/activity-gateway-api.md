---
"api": minor
---

Expose the activity gateway admin routes on the public API so status, mode, retry, and history
import are reachable at `/api/v1/internal/activity/gateway` instead of only on the plugin contract.
